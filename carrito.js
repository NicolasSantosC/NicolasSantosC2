const menuToggle=document.querySelector(".menu-toggle"),menu=document.querySelector("nav ul");function cargarCarrito(){let e=JSON.parse(localStorage.getItem("carrito"))||[],t=document.querySelector(".carrito-items"),r=document.getElementById("total");t.innerHTML="";let a=0;e.forEach(e=>{let r=document.createElement("div");r.classList.add("carrito-item"),r.innerHTML=`
    <img src="${e.img}" alt="${e.nombre}">
    <span>${e.nombre} - $${e.precio}</span>
    <button class="eliminar-item" data-nombre="${e.nombre}">Eliminar</button>
`,t.appendChild(r),a+=e.precio}),r.innerText=a.toFixed(2)}function vaciarCarrito(){localStorage.removeItem("carrito"),cargarCarrito()}menuToggle.addEventListener("click",()=>{menu.classList.toggle("active")}),document.getElementById("vaciar-carrito").addEventListener("click",vaciarCarrito),document.addEventListener("click",function(e){if(e.target.classList.contains("eliminar-item")){let t=e.target.dataset.nombre,r=JSON.parse(localStorage.getItem("carrito"))||[];r=r.filter(e=>e.nombre!==t),localStorage.setItem("carrito",JSON.stringify(r)),cargarCarrito()}}),document.addEventListener("DOMContentLoaded",cargarCarrito),document.getElementById("realizar-pago").addEventListener("click",function(){let e=document.getElementById("mensaje-pago");e.style.display="block",this.style.display="none"});